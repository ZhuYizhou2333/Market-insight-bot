[project]
name = "market-insight-bot"
version = "0.1.0"
description = "Market insight bot for analyzing news and market data from Telegram and Binance"
requires-python = ">=3.12"
dependencies = [
    "telethon",
    "pyzmq",
    "python-binance",
    "loguru>=0.7.3",
    "dashscope>=1.24.9",
    "markdown>=3.9",
]
